apiVersion: v1
kind: Service
metadata:
  name: {{ include "memgpt.fullname" . }}
  labels:
    {{- include "memgpt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "memgpt.selectorLabels" . | nindent 4 }}
