apiVersion: v1
kind: Service
metadata:
  name: {{ include "memgpt-server.fullname" . }}
  labels:
    {{- include "memgpt-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "memgpt-server.selectorLabels" . | nindent 4 }}
