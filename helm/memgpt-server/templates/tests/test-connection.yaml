apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "memgpt-server.fullname" . }}-test-connection"
  labels:
    {{- include "memgpt-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "memgpt-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
