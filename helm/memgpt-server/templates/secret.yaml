apiVersion: v1
kind: Secret
metadata:
  name: {{ include "memgpt-server.fullname" . }}-env-secret
  labels:
    {{- include "memgpt-server.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
